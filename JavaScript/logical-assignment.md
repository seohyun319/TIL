- `||=`, `&&=`, `??=`
    - **Logical OR assignment (`||=`)**
        - `x ||= y` === `x || (x = y)`
    - **Logical AND assignment (`&&=`)**
        - `x &&= y` === `x && (x = y)`
    - **Nullish coalescing assignment (`??=`)**
        - `x ??= y` === `x ?? (x = y)`
